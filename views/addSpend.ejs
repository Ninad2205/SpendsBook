<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Spend Book</title>
    <link rel="icon" type="image/png" href="./images/icon.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: #f0f0f0;
            color: #333; 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }

        .form-container {
            background: rgba(255, 255, 255, 0.8); 
            backdrop-filter: blur(8px);
            border-radius: 15px;
            border: 1px solid rgba(0, 0, 0, 0.1);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            padding: 30px;
            width: 100%;
            max-width: 500px;
            
        }

        .form-title {
            color: #007bff; 
            text-align: center;
            margin-bottom: 20px;
            font-size: 1.5rem;
            font-weight: bold;
        }

        .form-group {
            margin-bottom: 15px; 
        }

        .submitBtn {
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 10px;
            padding: 12px;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.2s ease;
            width: 100%;
        }

        .submitBtn:hover {
            background-color: #0056b3;
            transform: translateY(-2px);
        }

        .submitBtn:active {
            background-color: #003d7a;
            transform: translateY(0);
        }

        @media (max-width: 768px) {
            .form-container {
                padding: 20px;
            }
        }

        @media (max-width: 576px) {
            .form-container {
                padding: 15px;
            }

            .form-title {
                font-size: 1.25rem;
            }

        @media (max-width: 768px) {
            .form-container {
                padding: 20px;
            }
        }

        @media (max-width: 576px) {
            .form-container {
                padding: 15px;
            }

            .form-title {
                font-size: 1.25rem;
            }
        }
        }
    </style>
</head>

<body>
    <div class="container d-flex align-items-center justify-content-center min-vh-100">
        <div class="form-container p-4">
            <h1 class="form-title">Add New Spend</h1>
            <form method="post" action="/addSpend">
                <input type="hidden" name="userId" value="<%= userId %>">
                <div class="form-group">
                    <label for="nameOfItem">Item Name:</label>
                    <input type="text" id="nameOfItem" name="nameOfItem" class="form-control" placeholder="Enter Item Name">
                </div>
                <div class="form-group">
                    <label for="price">Price:</label>
                    <input type="text" id="price" name="price" class="form-control" placeholder="Enter Price">
                </div>
                <div class="form-group">
                    <label for="paymentMode">Payment Mode:</label>
                    <select id="paymentMode" name="paymentMode" class="form-control">
                        <option value="gpay">Gpay</option>
                        <option value="phonepe">Phone Pe</option>
                        <option value="fampay">Fampay</option>
                        <option value="cash">Cash</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="date">Date:</label>
                    <input type="date" id="date" name="date" class="form-control">
                </div>
                <button type="submit" class="submitBtn btn btn-primary">Submit</button>
            </form>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>
